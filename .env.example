# ChatterBox AI - Environment Configuration
#
# SECURITY: API keys must NEVER be stored in client-side code.
# This app requires a backend proxy to handle AI API calls securely.
#
# See README.md for setup instructions.
#
# Required backend environment variables (server-side only):
# GOOGLE_API_KEY=your_google_generative_ai_key_here
